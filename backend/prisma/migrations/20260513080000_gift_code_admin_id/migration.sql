-- T-promo (13.05.2026, WolfVPN): id админа/менеджера который создал подарочный код.
-- Используется чтобы в админ-списке подписок отображать конкретного менеджера в колонке «Отправитель».
-- FK не ставим — таблица admins может быть пересоздана, при удалении админа поле останется (для истории).

ALTER TABLE "gift_codes"
  ADD COLUMN IF NOT EXISTS "created_by_admin_id" TEXT;
