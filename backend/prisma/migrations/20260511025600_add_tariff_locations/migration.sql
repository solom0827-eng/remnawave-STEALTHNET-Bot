-- T11+T12 (11.05.2026, WolfVPN): rich-text список локаций для каждого тарифа.
-- Показывается клиенту по кнопке «🌐 Локации» в боте (детали подписки / после триала).
-- Пустое значение — кнопка «Локации» не появляется.
ALTER TABLE "tariffs" ADD COLUMN "locations" TEXT;
